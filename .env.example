# Redis queue — internal to the server (Docker network only, no public port needed)
REDIS_URL=redis://default:<password>@<host>:<port>/0

# GitHub webhook secret (set the same value in each repo's webhook settings)
GITHUB_WEBHOOK_SECRET=your-secret-here

# Shared secret between server and poller for the /poll endpoint
POLL_SECRET=generate-with-openssl-rand-hex-32

# Server port (index.ts only)
PORT=3000

# Public URL of the Hermod server (poller.ts only)
HERMOD_URL=https://hermod.yourdomain.com

# Paperclip (poller.ts only)
PAPERCLIP_API_URL=http://127.0.0.1:3100
PAPERCLIP_COMPANY_ID=your-company-id
PAPERCLIP_CEO_AGENT_ID=your-ceo-agent-id

# Comma-separated repo:agent-id pairs (poller.ts only)
REPO_AGENT_MAP=org/repo1:agent-id-1,org/repo2:agent-id-2

# Poll interval in ms (default: 30000)
POLL_INTERVAL_MS=30000
